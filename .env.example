# Public app URL used for SEO canonical and metadataBase.
# Example (production): https://nextcommerce.example.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Base API URL for products and authentication endpoints.
# Keep /api/v1 in the base to avoid repeating it in code.
NEXT_PUBLIC_API_BASE_URL=https://api.escuelajs.co/api/v1
